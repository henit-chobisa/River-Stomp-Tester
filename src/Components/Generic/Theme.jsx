import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary : {
        main : "#00A0C6"
      },
      secondary : {
        main : "#F53D3D"
      }
    }
});

export default theme;